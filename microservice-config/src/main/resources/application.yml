server:
  port: 8888

spring:
  config:
    import: optional:configserver:instead
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
      enabled: false
      