server:
  port: 0  # El puerto ser� asignado din�micamente

spring:
  application:
    name: msvc-pago  # Nombre del microservicio
  config:
    import: optional:configserver:http://config-server:8888  # Importar configuraci�n desde el Config Server

